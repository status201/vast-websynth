// Typed entry point for the vendored jsQR decoder (Cosmo Wolfe, Apache-2.0 — see
// ./LICENSE). Committed as a bundled asset, NOT an npm runtime dependency.
export { default as jsQR } from './jsQR.js';
export type { QRCode } from './jsQR.js';
