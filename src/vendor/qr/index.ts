// Typed entry point for the vendored qrcode-generator (Kazuhiko Arase, MIT — see
// ./LICENSE). Committed as a bundled asset, NOT an npm runtime dependency.
export { qrcode } from './qrcode.js';
export type { QrCode } from './qrcode.js';
