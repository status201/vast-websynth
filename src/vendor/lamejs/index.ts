// Typed entry point for the vendored lamejs MP3 encoder.
// lamejs is MIT-licensed (see ./LICENSE); it is committed into the repo as a
// bundled asset, NOT an npm runtime dependency.
export { Mp3Encoder } from './lame.min.js';
