<!-- GENERATED by `npm run gen:params` from src/state/params.ts — do not edit by hand. -->

# VAST G1-J8 — parameter reference

Every parameter the synth registers, with its range, default and — for a choice
parameter — its value map. The stored value of a choice parameter is the
**index** into that map. Values outside a range are clamped on load.

A **preset** (`websynth-preset`) carries the sound parameters below; a **song**
(`websynth-song`) carries those *plus* the song-level ones. Both use a sparse
map of `"<id>": number`, so you only set what matters.

`filter.cutoff` is a MIDI note number, **not** Hz — keep modulation additive in
semitone space.

Machine-readable: [`params.json`](params.json) — the same list, plus each
parameter's `taper`, `curve` and `unit`. File formats, versions and JSON
Schemas: [`llms.txt`](llms.txt).

## Sound parameters (what a preset carries)

- "voicing.mode": number  // range 0..1, default 1, step 1, values 0=mono 1=poly
- "osc1.wave": number  // range 0..3, default 2, step 1, values 0=sine 1=triangle 2=saw 3=square
- "osc1.octave": number  // range -2..2, default 0, step 1
- "osc1.detune": number  // range -50..50, default 0
- "osc1.level": number  // range 0..1, default 0.7
- "osc1.pulseWidth": number  // range 0.5..0.95, default 0.5
- "osc2.wave": number  // range 0..3, default 2, step 1, values 0=sine 1=triangle 2=saw 3=square
- "osc2.octave": number  // range -2..2, default 0, step 1
- "osc2.detune": number  // range -50..50, default 7
- "osc2.level": number  // range 0..1, default 0.5
- "osc2.pulseWidth": number  // range 0.5..0.95, default 0.5
- "sub.wave": number  // range 0..3, default 0, step 1, values 0=sine 1=triangle 2=saw 3=square
- "sub.octave": number  // range -2..-1, default -1, step 1
- "sub.level": number  // range 0..1, default 0
- "unison.voices": number  // range 1..4, default 1, step 1, values 0=off 1=2 2=3 3=4
- "unison.detune": number  // range 0..50, default 12
- "analog.drift": number  // range 0..1, default 0
- "mixer.noise": number  // range 0..1, default 0
- "mixer.glide": number  // range 0..1, default 0
- "glide.mode": number  // range 0..2, default 1, step 1, values 0=off 1=always 2=legato
- "filter.cutoff": number  // range 30..130, default 90
- "filter.resonance": number  // range 0..4.2, default 0.5
- "filter.drive": number  // range 0.5..6, default 1.2
- "filter.envAmount": number  // range -48..48, default 24
- "filter.velAmount": number  // range 0..1, default 0
- "filter.model": number  // range 0..1, default 0, step 1, values 0=ladder 1=poly
- "filter.shape": number  // range 0..1, default 0
- "filter.keytrack": number  // range 0..1, default 0
- "env.amp.attack": number  // range 0.001..4, default 0.005
- "env.amp.decay": number  // range 0.001..4, default 0.2
- "env.amp.sustain": number  // range 0..1, default 0.8
- "env.amp.release": number  // range 0.001..6, default 0.4
- "env.fil.attack": number  // range 0.001..4, default 0.005
- "env.fil.decay": number  // range 0.001..4, default 0.6
- "env.fil.sustain": number  // range 0..1, default 0.2
- "env.fil.release": number  // range 0.001..6, default 0.4
- "lfo.rate": number  // range 0.05..20, default 4
- "lfo.amount": number  // range 0..1, default 0
- "lfo.wave": number  // range 0..3, default 0, step 1, values 0=sine 1=triangle 2=saw 3=square
- "lfo.dest": number  // range 0..6, default 0, step 1, values 0=off 1=cutoff 2=pitch 3=amp 4=pulse 5=pan 6=shape
- "lfo.sync": number  // range 0..18, default 0, step 1, values 0=free 1=1/1 2=1/1 D 3=1/1 T 4=1/2 5=1/2 D 6=1/2 T 7=1/4 8=1/4 D 9=1/4 T 10=1/8 11=1/8 D 12=1/8 T 13=1/16 14=1/16 D 15=1/16 T 16=1/32 17=1/32 D 18=1/32 T
- "lfo2.rate": number  // range 0.05..20, default 4
- "lfo2.amount": number  // range 0..1, default 0
- "lfo2.wave": number  // range 0..3, default 0, step 1, values 0=sine 1=triangle 2=saw 3=square
- "lfo2.dest": number  // range 0..6, default 0, step 1, values 0=off 1=cutoff 2=pitch 3=amp 4=pulse 5=pan 6=shape
- "lfo2.sync": number  // range 0..18, default 0, step 1, values 0=free 1=1/1 2=1/1 D 3=1/1 T 4=1/2 5=1/2 D 6=1/2 T 7=1/4 8=1/4 D 9=1/4 T 10=1/8 11=1/8 D 12=1/8 T 13=1/16 14=1/16 D 15=1/16 T 16=1/32 17=1/32 D 18=1/32 T
- "fx.dist.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.dist.drive": number  // range 0..1, default 0.3
- "fx.dist.tone": number  // range 200..8000, default 3000
- "fx.dist.mix": number  // range 0..1, default 1
- "fx.wah.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.wah.rate": number  // range 0.05..10, default 2
- "fx.wah.depth": number  // range 0..1, default 0.4
- "fx.wah.q": number  // range 0.5..20, default 4
- "fx.phaser.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.phaser.rate": number  // range 0.05..5, default 0.5
- "fx.phaser.depth": number  // range 0..1, default 0.5
- "fx.phaser.feedback": number  // range 0..0.9, default 0.4
- "fx.phaser.mix": number  // range 0..1, default 0.5
- "fx.delay.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.delay.time": number  // range 0.01..1.5, default 0.35
- "fx.delay.feedback": number  // range 0..0.95, default 0.4
- "fx.delay.mix": number  // range 0..1, default 0.3
- "fx.reverb.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.reverb.size": number  // range 0..1, default 0.6
- "fx.reverb.damp": number  // range 0..1, default 0.4
- "fx.reverb.mix": number  // range 0..1, default 0.25
- "fx.master.comp.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.master.comp.threshold": number  // range -40..0, default -12
- "fx.master.comp.ratio": number  // range 0..2, default 1, step 1, values 0=2:1 1=4:1 2=10:1
- "fx.master.comp.attack": number  // range 0.0001..0.03, default 0.01
- "fx.master.comp.release": number  // range 0..4, default 4, step 1, values 0=0.1s 1=0.3s 2=0.6s 3=1.2s 4=auto
- "fx.master.comp.makeup": number  // range 0..12, default 0
- "master.volume": number  // range 0..1, default 0.8
- "motion.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "motion.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "motion.slide": number  // range 0..1, default 1, step 1, values 0=step 1=slide
- "motion.t0.slide": number  // range 0..1, default 1, step 1, values 0=step 1=slide
- "motion.t1.slide": number  // range 0..1, default 1, step 1, values 0=step 1=slide

## Song-only parameters (transport, arpeggiator, sequencer, drums, sampler)

- "fx.drum.phaser.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.drum.phaser.rate": number  // range 0.05..5, default 0.5
- "fx.drum.phaser.depth": number  // range 0..1, default 0.7
- "fx.drum.phaser.feedback": number  // range 0..0.9, default 0.4
- "fx.drum.phaser.mix": number  // range 0..1, default 0.6
- "fx.drum.delay.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.drum.delay.time": number  // range 0.01..1.5, default 0.35
- "fx.drum.delay.feedback": number  // range 0..0.95, default 0.4
- "fx.drum.delay.mix": number  // range 0..1, default 0.3
- "fx.drum.reverb.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.drum.reverb.size": number  // range 0..1, default 0.6
- "fx.drum.reverb.damp": number  // range 0..1, default 0.4
- "fx.drum.reverb.mix": number  // range 0..1, default 0.25
- "fx.drum.comp.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.drum.comp.threshold": number  // range -40..0, default -18
- "fx.drum.comp.ratio": number  // range 0..4, default 0, step 1, values 0=4:1 1=8:1 2=12:1 3=20:1 4=ALL
- "fx.drum.comp.attack": number  // range 0.00002..0.0008, default 0.0002
- "fx.drum.comp.release": number  // range 0.05..1.1, default 0.25
- "fx.drum.comp.makeup": number  // range 0..24, default 0
- "fx.sampler.dist.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.sampler.dist.drive": number  // range 0..1, default 0.3
- "fx.sampler.dist.tone": number  // range 200..8000, default 3000
- "fx.sampler.dist.mix": number  // range 0..1, default 1
- "fx.sampler.phaser.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.sampler.phaser.rate": number  // range 0.05..5, default 0.5
- "fx.sampler.phaser.depth": number  // range 0..1, default 0.7
- "fx.sampler.phaser.feedback": number  // range 0..0.9, default 0.4
- "fx.sampler.phaser.mix": number  // range 0..1, default 0.5
- "fx.sampler.delay.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.sampler.delay.time": number  // range 0.01..1.5, default 0.35
- "fx.sampler.delay.feedback": number  // range 0..0.95, default 0.4
- "fx.sampler.delay.mix": number  // range 0..1, default 0.3
- "fx.sampler.reverb.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "fx.sampler.reverb.size": number  // range 0..1, default 0.6
- "fx.sampler.reverb.damp": number  // range 0..1, default 0.4
- "fx.sampler.reverb.mix": number  // range 0..1, default 0.25
- "fx.djfilter": number  // range -1..1, default 0
- "master.pitchBend": number  // range -1..1, default 0
- "master.modWheel": number  // range 0..1, default 0
- "keyboard.transpose": number  // range -2..2, default 0, step 1
- "transport.bpm": number  // range 40..240, default 120, step 1
- "transport.swing": number  // range 0..1, default 0
- "arp.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "arp.pattern": number  // range 0..4, default 0, step 1, values 0=up 1=down 2=updn 3=rand 4=play
- "arp.rate": number  // range 0..3, default 2, step 1, values 0=1/4 1=1/8 2=1/16 3=1/32
- "arp.octaves": number  // range 1..4, default 1, step 1
- "arp.gate": number  // range 0.05..1, default 0.5
- "seq.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "seq.master": number  // range 0..1, default 1
- "seq.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "seq.t0.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "seq.t1.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "seq.t2.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "seq.t3.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "seq.solo": number  // range 0..1, default 0, step 1, values 0=off 1=solo
- "drum.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "drum.choke": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "drum.master": number  // range 0..1, default 0.85
- "drum.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "drum.solo": number  // range 0..1, default 0, step 1, values 0=off 1=solo
- "drum.t0.vol": number  // range 0..1, default 0.85
- "drum.t0.tune": number  // range -24..24, default 0, step 1
- "drum.t0.decay": number  // range 0.02..1.5, default 0.3
- "drum.t0.tone": number  // range 0..1, default 1
- "drum.t0.drive": number  // range 0..1, default 0
- "drum.t0.pan": number  // range -1..1, default 0
- "drum.t0.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "drum.t0.model": number  // range 0..12, default 0, step 1, values 0=Kick 1=Snare 2=C.Hat 3=O.Hat 4=L.Tom 5=M.Tom 6=H.Tom 7=Clap 8=Conga 9=Bongo 10=Cowbell 11=Clave 12=Shaker
- "drum.t1.vol": number  // range 0..1, default 0.85
- "drum.t1.tune": number  // range -24..24, default 0, step 1
- "drum.t1.decay": number  // range 0.02..1.5, default 0.3
- "drum.t1.tone": number  // range 0..1, default 1
- "drum.t1.drive": number  // range 0..1, default 0
- "drum.t1.pan": number  // range -1..1, default 0
- "drum.t1.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "drum.t1.model": number  // range 0..12, default 1, step 1, values 0=Kick 1=Snare 2=C.Hat 3=O.Hat 4=L.Tom 5=M.Tom 6=H.Tom 7=Clap 8=Conga 9=Bongo 10=Cowbell 11=Clave 12=Shaker
- "drum.t2.vol": number  // range 0..1, default 0.85
- "drum.t2.tune": number  // range -24..24, default 0, step 1
- "drum.t2.decay": number  // range 0.02..1.5, default 0.3
- "drum.t2.tone": number  // range 0..1, default 1
- "drum.t2.drive": number  // range 0..1, default 0
- "drum.t2.pan": number  // range -1..1, default 0
- "drum.t2.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "drum.t2.model": number  // range 0..12, default 2, step 1, values 0=Kick 1=Snare 2=C.Hat 3=O.Hat 4=L.Tom 5=M.Tom 6=H.Tom 7=Clap 8=Conga 9=Bongo 10=Cowbell 11=Clave 12=Shaker
- "drum.t3.vol": number  // range 0..1, default 0.85
- "drum.t3.tune": number  // range -24..24, default 0, step 1
- "drum.t3.decay": number  // range 0.02..1.5, default 0.3
- "drum.t3.tone": number  // range 0..1, default 1
- "drum.t3.drive": number  // range 0..1, default 0
- "drum.t3.pan": number  // range -1..1, default 0
- "drum.t3.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "drum.t3.model": number  // range 0..12, default 3, step 1, values 0=Kick 1=Snare 2=C.Hat 3=O.Hat 4=L.Tom 5=M.Tom 6=H.Tom 7=Clap 8=Conga 9=Bongo 10=Cowbell 11=Clave 12=Shaker
- "drum.t4.vol": number  // range 0..1, default 0.85
- "drum.t4.tune": number  // range -24..24, default 0, step 1
- "drum.t4.decay": number  // range 0.02..1.5, default 0.3
- "drum.t4.tone": number  // range 0..1, default 1
- "drum.t4.drive": number  // range 0..1, default 0
- "drum.t4.pan": number  // range -1..1, default 0
- "drum.t4.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "drum.t4.model": number  // range 0..12, default 4, step 1, values 0=Kick 1=Snare 2=C.Hat 3=O.Hat 4=L.Tom 5=M.Tom 6=H.Tom 7=Clap 8=Conga 9=Bongo 10=Cowbell 11=Clave 12=Shaker
- "drum.t5.vol": number  // range 0..1, default 0.85
- "drum.t5.tune": number  // range -24..24, default 0, step 1
- "drum.t5.decay": number  // range 0.02..1.5, default 0.3
- "drum.t5.tone": number  // range 0..1, default 1
- "drum.t5.drive": number  // range 0..1, default 0
- "drum.t5.pan": number  // range -1..1, default 0
- "drum.t5.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "drum.t5.model": number  // range 0..12, default 5, step 1, values 0=Kick 1=Snare 2=C.Hat 3=O.Hat 4=L.Tom 5=M.Tom 6=H.Tom 7=Clap 8=Conga 9=Bongo 10=Cowbell 11=Clave 12=Shaker
- "drum.t6.vol": number  // range 0..1, default 0.85
- "drum.t6.tune": number  // range -24..24, default 0, step 1
- "drum.t6.decay": number  // range 0.02..1.5, default 0.3
- "drum.t6.tone": number  // range 0..1, default 1
- "drum.t6.drive": number  // range 0..1, default 0
- "drum.t6.pan": number  // range -1..1, default 0
- "drum.t6.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "drum.t6.model": number  // range 0..12, default 6, step 1, values 0=Kick 1=Snare 2=C.Hat 3=O.Hat 4=L.Tom 5=M.Tom 6=H.Tom 7=Clap 8=Conga 9=Bongo 10=Cowbell 11=Clave 12=Shaker
- "drum.t7.vol": number  // range 0..1, default 0.85
- "drum.t7.tune": number  // range -24..24, default 0, step 1
- "drum.t7.decay": number  // range 0.02..1.5, default 0.3
- "drum.t7.tone": number  // range 0..1, default 1
- "drum.t7.drive": number  // range 0..1, default 0
- "drum.t7.pan": number  // range -1..1, default 0
- "drum.t7.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "drum.t7.model": number  // range 0..12, default 7, step 1, values 0=Kick 1=Snare 2=C.Hat 3=O.Hat 4=L.Tom 5=M.Tom 6=H.Tom 7=Clap 8=Conga 9=Bongo 10=Cowbell 11=Clave 12=Shaker
- "sampler.on": number  // range 0..1, default 0, step 1, values 0=off 1=on
- "sampler.master": number  // range 0..1, default 0.85
- "sampler.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "sampler.solo": number  // range 0..1, default 0, step 1, values 0=off 1=solo
- "sampler.t0.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "sampler.t1.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "sampler.t2.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "sampler.t3.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "sampler.t4.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "sampler.t5.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "sampler.t6.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
- "sampler.t7.mute": number  // range 0..1, default 0, step 1, values 0=on 1=mute
